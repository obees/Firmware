#!/bin/sh
#
# @name 3DR Iris Quadrotor SITL (Obstacle Avoidance)
#
# @type Quadrotor Wide
#

. ${R}etc/init.d-posix/airframes/10016_iris

if [ $AUTOCNF = yes ]
then
	param set COM_OBS_AVOID 0
	param set MPC_XY_CRUISE 6.0
	param set CP_DIST 10.0
fi
